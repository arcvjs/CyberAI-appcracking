<UserControl x:Class="Vapor.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#1B2838" Offset="0"/>
                <GradientStop Color="#16202D" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Border Width="380" VerticalAlignment="Center" HorizontalAlignment="Center" Background="#1F3243" CornerRadius="3" Padding="32,30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="VAPOR" FontFamily="Segoe UI Black" FontSize="26" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Sign in with your account name" FontSize="13" Foreground="{StaticResource SteamTextDim}" HorizontalAlignment="Center" Margin="0,6,0,22"/>
                <TextBlock Text="Account name" FontSize="12" Foreground="{StaticResource SteamText}" Margin="0,0,0,4"/>
                <TextBox x:Name="AccountBox" Height="32" Padding="6,4" Background="#0E1C26" BorderBrush="{StaticResource SteamBlueDark}" BorderThickness="1" Foreground="White" FontSize="14" Text="festivalgoer"/>
                <TextBlock Text="Password" FontSize="12" Foreground="{StaticResource SteamText}" Margin="0,14,0,4"/>
                <PasswordBox x:Name="PasswordBox" Height="32" Padding="6,4" Background="#0E1C26" BorderBrush="{StaticResource SteamBlueDark}" BorderThickness="1" Foreground="White" FontSize="14"/>
                <CheckBox x:Name="RememberBox" Content="Remember my password on this PC" IsChecked="True" FontSize="12" Foreground="{StaticResource SteamTextDim}" Margin="0,14,0,0"/>
                <Button x:Name="SignInButton" Content="Sign in" Style="{StaticResource PrimaryButton}" Margin="0,22,0,0" Click="OnSignIn"/>
                <TextBlock x:Name="ErrorText" Text="" FontSize="12" Foreground="{StaticResource SteamOrange}" Margin="0,14,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                <TextBlock Text="Saved account: festivalgoer" FontSize="11" Foreground="{StaticResource SteamTextDim}" Margin="0,18,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
